{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "pnpm -C ./packages/with-hooks run build",
    "watch": "pnpm -C ./packages/with-hooks run watch",
    "example": "pnpm -C ./packages/example run dev",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.0"
  }
}
